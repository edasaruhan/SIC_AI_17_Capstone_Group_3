# Reports

- `existing_coursework/` contains byte-for-byte copies of the supplied academic inputs.
- `generated/` contains engineering status reports produced during implementation.

The supplied documents are reference material. The user's explicit request and the
Sprint 1 boundary in `ARGUS_CODEX_MASTER_PROMPT.md` control this implementation.

